- type: log
  enabled: ${BLUECATS_DMS50:false}
  ignore_older: 48h
  fields_under_root: true
  fields: 
    tag: "logs.dms.50"  
  tags: ["DMS50"]
  harvester_limitedit: 1
  multiline.pattern: '^([^\^]*)\^([^\^]*)\^([^\^]*)\^([^\^]*)\^([^\^]*)\^([^\^]*)\^([^\^]*)\^([^\^]*)\^([^\^]*)\^([^\^]*)\^([^\^]*)\^([^\^]*)\^[1-9]'
  multiline.negate: true
  multiline.match: before
  paths:
    - ${BLUECATS_DMS50_ROOTDIR:d:/}/#_DM50_#/01.Log/*/Loader_*.log